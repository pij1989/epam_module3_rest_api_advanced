server:
  port: 8081
spring:
  messages:
    basename: messages
    encoding: UTF-8
  liquibase:
    enabled: true
    change-log: changeLog.xml
logging:
  level:
    org:
      springframework:
        transaction:
          interceptor: TRACE
    com:
      epam:
        esm:
          configuration:
            DevDatasourceConfiguration: TRACE
